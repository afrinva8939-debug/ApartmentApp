DB_HOST=localhost         # or Railway public host if using public connection
DB_PORT=3306
DB_USER=root
DB_PASS=root
DB_NAME=apartments
PORT=5000
# Optional: define FRONTEND_ORIGIN=http://localhost:3000 when NOT using CRA proxy
